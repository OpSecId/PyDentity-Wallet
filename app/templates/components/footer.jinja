<footer class="footer footer-transparent border-0 py-3 scan-footer">
    <div class="d-flex justify-content-center">
        <button
            id="videoBtn"
            type="button"
            class="btn btn-primary scan-fab rounded-circle border-0 shadow-lg"
            data-bs-toggle="modal"
            data-bs-target="#scanner-modal"
            aria-label="Scan QR code"
        >
            <i class="ti ti-qrcode"></i>
        </button>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@undecaf/zbar-wasm@0.11.0/dist/index.js"></script>
    <script src="{{url_for('static', filename='qr-scanner.js')}}"></script>
</footer>

<style>
    .scan-footer {
        padding-bottom: calc(0.75rem + env(safe-area-inset-bottom, 0px));
        background: linear-gradient(to top, var(--tblr-body-bg) 40%, transparent);
        pointer-events: none;
    }

    .scan-footer .scan-fab {
        pointer-events: auto;
        width: 3.5rem;
        height: 3.5rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .scan-footer .scan-fab .ti {
        font-size: 1.5rem;
        line-height: 1;
    }

    .scan-footer .scan-fab:hover,
    .scan-footer .scan-fab:focus-visible {
        transform: translateY(-1px);
        box-shadow: 0 8px 24px rgba(var(--tblr-primary-rgb), 0.45) !important;
    }

    .scan-footer .scan-fab:active {
        transform: scale(0.94);
    }
</style>
